# Setup my dotfiles for MacOS

## Prerequisites

* git is installed via homebrew

## ZSH
    $ cd ~
    $ ln -s $DOTFILES/.zsh .
    $ ln -s $DOTFILES/.zsh_rc .
    $ ln -s $DOTFILES/.tm_properties .
